# n8n 工作流展示平台

> 探索 2000+ n8n 自动化工作流模板，涵盖 AI、社交媒体、电商、SEO 等多个领域，一键导入到你的 n8n 实例。

![Next.js](https://img.shields.io/badge/Next.js-14.2.33-black?logo=next.js)
![React](https://img.shields.io/badge/React-18.3.1-blue?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5.x-blue?logo=typescript)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-3.4.15-38B2AC?logo=tailwind-css)
![Framer Motion](https://img.shields.io/badge/Framer_Motion-11.11.11-purple?logo=framer)

---

## 项目概述

**n8n-showcase** 是一个现代化的 n8n 工作流模板展示平台，帮助用户快速发现、浏览和导入适合自己业务场景的自动化工作流。

### 核心特性

- **2000+ 工作流模板** - 覆盖 AI/LLM、社交媒体、跨境电商、SEO、邮件自动化等 15+ 场景分类
- **智能分类系统** - 基于文件名、节点类型、目录结构的多维度自动分类算法
- **强大的搜索过滤** - 支持按名称、标签、节点类型搜索，多标签组合筛选
- **一键导入** - 直接复制 JSON 或下载文件，快速导入到 n8n 实例
- **深色/浅色主题** - 支持主题切换，适配不同使用偏好
- **响应式设计** - 完美适配桌面端和移动端

---

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Next.js** | 14.2.33 | React 框架 (App Router + 静态导出) |
| **React** | 18.3.1 | UI 组件库 |
| **TypeScript** | 5.x | 类型安全 |
| **Tailwind CSS** | 3.4.15 | 原子化样式框架 |
| **Framer Motion** | 11.11.11 | 动画效果 |
| **Lucide React** | 0.460.0 | 图标库 |

---

## 项目结构

```
n8n-showcase/
├── app/                          # Next.js App Router
│   ├── layout.tsx               # 根布局（元数据、语言设置）
│   ├── page.tsx                 # 主页面（工作流列表、搜索、筛选）
│   └── globals.css              # 全局样式（n8n 主题色、动画）
├── components/                   # React 组件
│   ├── layout/
│   │   ├── Header.tsx           # 顶部导航（标签页、主题切换）
│   │   └── Footer.tsx           # 页脚
│   ├── home/
│   │   ├── CategoryGrid.tsx     # 分类网格展示
│   │   ├── ApplySection.tsx     # 申请体验页
│   │   ├── ServicesSection.tsx  # 推荐服务页
│   │   ├── DonateModal.tsx      # 赞赏弹窗
│   │   └── ...
│   ├── workflow/
│   │   └── ImportModal.tsx      # 工作流导入弹窗
│   └── ui/
│       └── Modal.tsx            # 通用弹窗组件
├── data/                         # 生成的数据文件
│   └── workflows.json           # 工作流索引数据
├── lib/                          # 工具库
│   └── types.ts                 # TypeScript 类型定义
├── public/                       # 静态资源
│   ├── workflows/               # 工作流 JSON 文件
│   ├── data/                    # 公开数据文件
│   └── images/                  # 图片资源
├── scripts/                      # 构建脚本
│   └── generate-workflows-data.js  # 工作流数据生成脚本
├── package.json
├── next.config.js               # Next.js 配置（静态导出）
├── tailwind.config.ts           # Tailwind 配置
└── tsconfig.json                # TypeScript 配置
```

---

## 快速开始

### 环境要求

- Node.js 18.x 或更高版本
- npm 或 yarn

### 安装依赖

```bash
cd n8n-showcase
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000 查看开发版本。

### 生产构建

```bash
npm run build
```

构建过程会：
1. 运行 `generate-workflows-data.js` 脚本生成工作流索引
2. 复制工作流文件到 `public/workflows/` 目录
3. 执行 Next.js 静态导出到 `out/` 目录

### 预览构建结果

```bash
npm run start
```

---

## 命令说明

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 生成数据 + 构建静态站点 |
| `npm run start` | 启动生产服务器 |
| `npm run lint` | ESLint 代码检查 |
| `npm run generate-data` | 单独生成工作流数据 |

---

## 工作流分类系统

项目使用智能分类算法，自动将工作流归类到 15+ 场景类别：

### 社交媒体平台（优先级最高）

| 分类 | 图标 | 关键词 |
|------|------|--------|
| Twitter/X | 𝕏 | twitter, tweet, x.com |
| Telegram | ✈️ | telegram, tg-bot |
| YouTube | ▶️ | youtube, video |
| Instagram | 📸 | instagram, insta |
| Discord | 💬 | discord |
| WhatsApp | 💚 | whatsapp |
| Slack | 💼 | slack |

### 业务场景

| 分类 | 图标 | 关键词 |
|------|------|--------|
| AI/LLM 应用 | 🤖 | ai, gpt, openai, agent, rag |
| 跨境电商 | 🛒 | shopify, amazon, order |
| 内容创作 | ✍️ | content, blog, article |
| 数据处理 | 📊 | data, etl, sync, database |
| 发票处理 | 🧾 | invoice, billing, payment |
| SEO 优化 | 🔍 | seo, keyword, serp |
| 邮件自动化 | 📧 | email, gmail, newsletter |
| 文档处理 | 📄 | pdf, document, excel |
| 项目管理 | 📋 | notion, trello, jira |
| CRM/销售 | 💰 | crm, hubspot, salesforce |

### 分类算法

分类基于多维度评分：

1. **文件名关键词匹配** (权重: 10)
2. **节点类型匹配** (权重: 8)
3. **目录结构匹配** (权重: 6-15)
4. **分类优先级加权** (社交媒体 > 业务场景 > 其他)

---

## 功能模块

### 1. 工作流浏览

- **分类网格** - 按场景快速筛选
- **关键词搜索** - 支持名称、标签、节点类型
- **标签过滤** - 多标签组合筛选
- **分页加载** - 每次加载 60 个，按需加载更多

### 2. 工作流详情

每个工作流卡片展示：
- 工作流名称和描述
- 分类图标和名称
- 节点数量和类型（彩色标签）
- 标签列表
- 一键导入按钮

### 3. 导入功能

点击"导入"按钮打开弹窗：
- 复制完整 JSON 到剪贴板
- 下载 JSON 文件
- 查看导入指南

### 4. 主题切换

支持深色/浅色主题：
- 深色模式：n8n 风格暗色主题
- 浅色模式：清爽白色主题
- 自动持久化用户偏好

---

## 数据生成脚本

`scripts/generate-workflows-data.js` 是核心数据生成脚本：

### 功能

1. 扫描指定目录的 JSON 工作流文件
2. 解析每个工作流的节点信息
3. 应用智能分类算法
4. 提取标签（从节点类型和文件名）
5. 生成唯一 ID（Base64 编码）
6. 输出索引到 `data/workflows.json`
7. 复制工作流文件到 `public/workflows/`

### 配置工作流来源

修改脚本中的 `WORKFLOW_SOURCES` 数组：

```javascript
const WORKFLOW_SOURCES = [
  {
    path: '../path/to/your/workflows',
    source: 'custom'
  }
];
```

### 运行脚本

```bash
npm run generate-data
```

---

## 部署

### 静态导出

项目配置为静态导出模式 (`output: 'export'`)，可部署到任何静态托管服务：

- **Cloudflare Pages**
- **Vercel**
- **Netlify**
- **GitHub Pages**
- **AWS S3 + CloudFront**

### 部署步骤

```bash
# 1. 构建
npm run build

# 2. 部署 out/ 目录到静态托管服务
```

### Cloudflare Pages 示例

```bash
npx wrangler pages deploy out --project-name=n8n-showcase
```

---

## 自定义配置

### 修改主题色

编辑 `app/globals.css`：

```css
:root {
  --n8n-orange: #FF6D5A;  /* 主色调 */
  --n8n-dark: #1a1a2e;    /* 深色背景 */
  --n8n-darker: #0f0f1a;  /* 更深背景 */
}
```

### 修改 Tailwind 配置

编辑 `tailwind.config.ts` 添加自定义颜色和样式。

### 添加新分类

编辑 `scripts/generate-workflows-data.js` 中的 `CATEGORIES` 数组：

```javascript
{
  id: 'new-category',
  name: 'New Category',
  nameZh: '新分类',
  icon: '🆕',
  color: 'from-blue-500 to-blue-700',
  keywords: ['keyword1', 'keyword2'],
  nodePatterns: ['nodeType1', 'nodeType2'],
  priority: 2
}
```

---

## 相关资源

- [n8n 官方文档](https://docs.n8n.io/)
- [n8n 官方模板库](https://n8n.io/workflows/)
- [n8n GitHub](https://github.com/n8n-io/n8n)

---

## 许可证

MIT License

---

## 贡献

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

---

**Made with ❤️ for the n8n community**
